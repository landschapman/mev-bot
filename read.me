# One terminal only
$env:DASH_ENABLE="true"; node dist/index.js --interval=60; npm start      #run both bot and local host
npm start          # runs node dist/index.mjs

$env:DASH_ENABLE='true'
node dist/index.js --interval=60

$env:DASH_ENABLE="true"; node dist/index.js --interval=60; npm start

npm run release

npm run test-run